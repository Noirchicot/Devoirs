# Devoirs — fiches de Leto

Ouvrir `index.html` dans un navigateur, puis choisir une série dans le menu.

- toucher la carte pour afficher la traduction ;
- utiliser les flèches pour passer d’une fiche à l’autre.

Le vocabulaire se trouve dans le tableau `cards` à la fin de `fiches-allemand.html`.
